# HEST-VALIS dependencies.
#
# This pipeline runs across THREE environments (see env/setup.md); they are NOT installable
# together (the registration env uses the HEST `valis_hest` fork + numpy 2.x, while the StarDist
# env uses numpy 1.x). Exact pins below reflect a known-good configuration -- minor-version drift
# in valis / stardist / tensorflow / numpy is the most likely thing to change results, so pin
# those per env rather than relying on lower bounds.

# --- registration env (valis) ---
# Preferred: the HEST `valis_hest` fork (install via HEST, see env/setup.md), NOT pip valis-wsi.
# If you instead use upstream pip valis, pin it, e.g.:
# valis-wsi==1.1.0
pyvips==2.2.3
jpype1==1.5.0

# --- StarDist env (H&E nuclei) ---  [known-good]
stardist==0.9.2
tensorflow==2.15.1
csbdeep>=0.7.4
numpy==1.26.4            # StarDist/TF env pins numpy 1.x

# --- QC / general env (metrics, selection, annotation) ---
scipy>=1.9.0
scikit-image>=0.19.0
scikit-learn>=1.1.0
tifffile>=2022.2.9
zarr>=2.13.0
pandas>=1.5.0
matplotlib>=3.5.0
# numpy in this env follows whichever of the registration (2.x) / StarDist (1.x) env you reuse.
